# Next Steps

1. Add editable settings for provider keys and LM Studio URL.
2. Implement SQLite repository using `data/db/schema.sql`.
3. Replace sample catalog with SQLite-backed media data.
4. Add TMDB/OMDb/Spotify clients behind `integrations/`.
5. Add MiniLM semantic similarity as a replacement for the current fallback similarity engine.
6. Add tests for intent parsing, scoring, provider fallback, and SQLite repositories.
